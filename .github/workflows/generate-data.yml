name: Generate data (manual)

on:
  workflow_dispatch:
    inputs:
      universe_kr:
        description: "KR universe limit (e.g. 200)"
        required: false
        default: "200"
      universe_us:
        description: "US universe limit (e.g. 100)"
        required: false
        default: "100"
      enable_news:
        description: "Enable news collection (1/0)"
        required: false
        default: "1"
      enable_fundamentals:
        description: "Enable fundamentals collection (1/0)"
        required: false
        default: "1"

permissions:
  contents: write

concurrency:
  group: generate-data
  cancel-in-progress: true

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend deps
        run: |
          python -m pip install --upgrade pip
          pip install -r web/backend/requirements.txt

      - name: Generate JSON snapshots
        env:
          # Use an ephemeral SQLite DB inside the runner workspace (do NOT commit DB files).
          DATABASE_URL: sqlite:///./data/ci.db
          ENABLE_NEWS: ${{ github.event.inputs.enable_news }}
          ENABLE_FUNDAMENTALS: ${{ github.event.inputs.enable_fundamentals }}
          UNIVERSE_LIMIT_KR: ${{ github.event.inputs.universe_kr }}
          UNIVERSE_LIMIT_US: ${{ github.event.inputs.universe_us }}
          # Optional provider keys (set as GitHub Secrets if you have them)
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
          ALPHAVANTAGE_API_KEY: ${{ secrets.ALPHAVANTAGE_API_KEY }}
        run: |
          cd web/backend
          mkdir -p data
          python -m app.static_export

      - name: Commit generated data
        run: |
          git status
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add web/frontend/public/data
          git commit -m "Update data snapshots"
          git push

